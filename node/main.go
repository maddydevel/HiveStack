// HiveStack Node Agent
package main

import (
    "fmt"
    "os"
)

func main() {
    fmt.Println("HiveStack Node Agent — placeholder")
    fmt.Fprintln(os.Stderr, "Node agent not yet implemented")
    os.Exit(1)
}
